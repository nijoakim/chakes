# High priority:

# Medium priority:
+ engine: Refactor engine to have a position object instead of relying on tuples and string conversions

# Low priority:
+ engine: Generalize en passant moves (requires refactor)
+ engine: Support locusts (requires refactor)
+ engine: Add function to fill game state with all legal moves for all pieces
+ engine: winner() should be able to return a draw
- frontend: Add possibility to play non-orthodoxly-sized boards (testable for knighted chess)

# Bugs:
