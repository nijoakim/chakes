[project]
name = "engine"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
]

[dependency-groups]
dev = ["ruff", "ty", "mypy"]

[tool.mypy]
mypy_path = ".."
explicit_package_bases = true
strict = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
