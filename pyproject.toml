[project]
name = "chakes"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[project.scripts]
dev = "uvicorn chakes.server.app:app --reload"
test = "pytest"
